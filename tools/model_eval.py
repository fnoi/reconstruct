def model_evaluation(skeleton, something):
    for bone in skeleton.bones:
        print(bone)
        # compute distance between point cloud and obj mesh
        # report metrics and sicknice plotly plot (transparent beam, color coded points)
    # aggregate obj (either self or add option to blender)
    # compute overall distance point cloud to the full aggregated obj mesh

